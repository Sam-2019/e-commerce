input CreateUser {
  username: String
  firstName: String
  lastName: String
  email: EmailAddress
  password: String
  phoneNumber: Int
}

input AddProduct {
  name: String
  slug: String
  price: Float
  imageURL: String
  quantity: Int
  detail: String
  description: Markdown
}

input AddCart {
  productID: ID
  quantity: Int
}

input AddWishList {
  productID: ID
}

input AddOrder {
  products: [ID]
  paymentID: ID
  deliveryID: ID
  orderNumber: Int
  orderValue: Float
}

input AddReview {
  productID: ID
  rating: Float
  review: Markdown
}

input Login {
  email: EmailAddress
  password: String
}

input AddLocation {
  location: String
  fee: Float
}

input AddPayment {
  method: PaymentMethod
  status: StatusType
  phoneNumber: Int
}

input AddDelivery {
  address: DigitalAddress
  landmark: String
}
