type UserDetails implements User {
  userID: ID
  userName: String
  firstName: String
  lastName: String
  email: EmailAddress
  photoURL: String
  phoneNumber: Int
  verified: Boolean
  cart: [Cart]
  order: [Order]
  wishlist: [WishList]
  review: [Review]
}

type ProductDetails implements Product {
  productID: ID
  name: String
  shortName: String
  price: Float
  imageURL: String
  quantity: Int
  author: String
  description: Markdown
  rating: Float
  review: [Review]
}

type Cart {
  cartID: ID
  userID: ID
  productID: Product
  price: Float
  quantity: Int
}

type CartItem implements Product {
  cartID: ID
  quantity: Int
  productID: ID
  name: String
  shortName: String
  price: Float
  imageURL: String
}

type WishList {
  wishlistID: ID
  userID: ID
  productID: ID
}

type WishListItem implements Product {
  wishlistID: ID
  name: String
  shortName: String
  price: Float
  imageURL: String
}

type Order {
  orderID: ID
  userID: ID
  products: [ProductDetails]
  paymentID: ID
  deliveryID: ID
  orderNumber: Int
  orderValue: Float
  status: StatusType
}

type OrderItem implements Product {
  orderItemID: ID
  name: String
  shortName: String
  price: Float
  imageURL: String
  quantity: Int
  status: StatusType
}

type ReviewDetails implements Review {
  reviewID: ID
  user: User
  productID: ID
  review: Markdown
  rating: Float
  createdAt: String
}

type LocationDetails implements Location {
  locationID: ID
  disable: Boolean
  name: String
  fee: Float
}

type PaymentDetails implements Payment {
  paymentID: ID
  method: String
  status: StatusType
  phoneNumber: Int
  momoName: String
  momoNumber: Int
  momoTransactionID: String
}

type Cash {
  momoName: String
}

type MobileMoney {
  momoName: String
  momoNumber: Int
  momoTransactionID: String
}

type DeliveryDetails implements Delivery {
  deliveryID: ID
  locationID: ID
  digitalAddress: String
  landmark: String
}

type Pagination {
  count: Int
  products: [ProductDetails]
}

type AuthenticationData {
  userID: ID
  token: String
  tokenExpiration: Int
}